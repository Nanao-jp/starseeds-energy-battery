# アニメーション表示方法の調査結果

## 調査概要
カードやその他要素の表示アニメーションの現状を調査しました。

---

## 1. KpiSection / KpiCard

### アニメーション方式
- **コンテナレベル**: `staggerChildren` を使用
- **カードレベル**: 個別に `itemVariants` でアニメーション

### タイミング詳細
```typescript
// KpiSection (コンテナ)
containerVariants: {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.2  // 0.2秒間隔で各カードを順次表示
    }
  }
}

// KpiCard (個別カード)
itemVariants: {
  hidden: { y: 20, opacity: 0 },
  visible: {
    y: 0,
    opacity: 1,
    transition: { duration: 0.5 }
  }
}
```

### 評価
- **段階的表示**: 3枚のカードが0.2秒間隔で順次表示（0秒、0.2秒、0.4秒）
- **表示時間**: 最初のカードから最後まで約0.9秒
- **問題点**: 段階的な表示で、最後のカードが見えるまでに時間がかかる

---

## 2. FeatureCard

### アニメーション方式
- **カード全体**: 1つの `whileInView` アニメーション
- **内部要素**: 複数の個別アニメーション（順次表示）

### タイミング詳細
```typescript
// カード全体
initial={{ y: 20, opacity: 0 }}
whileInView={{ y: 0, opacity: 1 }}
transition={{ duration: 0.5 }}

// アイコン + ドット
- アイコン: delay: 0.2秒
- ドット1: delay: 0.3秒
- ドット2: delay: 0.4秒
- ドット3: delay: 0.5秒

// タイトル
delay: 0.1秒

// アンダーライン
delay: 0.5秒、duration: 0.8秒

// 説明文
delay: 0.3秒
```

### 評価
- **段階的表示**: カード全体 + 内部要素が0.1〜0.5秒の遅延で順次表示
- **表示時間**: カード表示後、内部要素が完全に見えるまで約1.3秒
- **問題点**: 内部の細かい要素（アイコン、ドット、タイトル、説明文）が個別にアニメーションするため、見え方に時間差がある

---

## 3. StrengthsSection

### アニメーション方式
- **セクション**: アニメーションなし（直接FeatureCardに委譲）
- **FeatureCard**: 上記と同じ（複数のFeatureCardが順次表示される）

### 評価
- **問題点**: FeatureCardが2枚あり、それぞれが上記の段階的アニメーションを実行

---

## 4. NavigationCardsSection

### アニメーション方式
- **セクションタイトル**: 複数の要素が順次表示
- **カードグリッド**: 全体が一括でフェードイン
- **個別カード**: さらに個別のアニメーション

### タイミング詳細
```typescript
// セクションタイトル全体
delay: 0秒、duration: 0.6秒

// アイコン
delay: 0.2秒、duration: 0.5秒

// タイトル
delay: 0.1秒、duration: 0.6秒

// アンダーライン
delay: 0.5秒、duration: 0.8秒

// 説明文
delay: 0.3秒、duration: 0.6秒

// カードグリッド全体
delay: 0.4秒、duration: 0.6秒

// 個別NavigationCard
delay: index * 0.1秒（0秒、0.1秒、0.2秒）
```

### 評価
- **段階的表示**: 
  - セクションタイトル: 0〜0.8秒
  - カードグリッド: 0.4秒後に開始
  - 個別カード: さらに0.1秒間隔で順次表示
- **総表示時間**: 約1.8秒（最後のカードが完全に表示されるまで）
- **問題点**: 多重の段階的表示で、全体が見えるまで時間がかかりすぎる

---

## 5. NavigationCard

### アニメーション方式
- **カード全体**: `index * 0.1` で順次表示
- **タイトル下のアンダーライン**: さらに個別アニメーション

### タイミング詳細
```typescript
// カード全体
initial={{ opacity: 0, y: 20 }}
whileInView={{ opacity: 1, y: 0 }}
transition={{ duration: 0.5, delay: index * 0.1 }}

// アンダーライン
delay: 0.2秒、duration: 0.6秒
```

### 評価
- **段階的表示**: 3枚のカードが0.1秒間隔で表示
- **問題点**: NavigationCardsSectionのグリッドアニメーション（0.4秒）と、個別カードのアニメーション（0〜0.2秒）が重複している

---

## 6. Footer

### アニメーション方式
- **各セクション**: 個別に順次表示

### タイミング詳細
```typescript
// 会社情報セクション
delay: 0秒、duration: 0.5秒

// 連絡先情報セクション
delay: 0.1秒、duration: 0.5秒

// ナビゲーションセクション
delay: 0.2秒、duration: 0.5秒

// コピーライト
delay: 0.3秒、duration: 0.5秒
```

### 評価
- **段階的表示**: 4つの要素が0.1秒間隔で順次表示
- **表示時間**: 約0.8秒
- **問題点**: Footerまで段階的に表示されると、スクロールが完了するまで全体が見えない

---

## 総合評価

### 現在の問題点

1. **多重の段階的アニメーション**
   - NavigationCardsSection: セクションタイトル → グリッド → 個別カード（3層）
   - FeatureCard: カード → アイコン → ドット → タイトル → 説明文（5層）
   - 合計で非常に長い時間がかかる

2. **staggerChildren の多用**
   - KpiSection: 0.2秒間隔
   - NavigationCard: index * 0.1秒間隔
   - 複数カードが順次表示され、最後まで時間がかかる

3. **delay の累積**
   - NavigationCardsSection: セクションタイトル（0.8秒）→ グリッド（0.4秒遅延）→ カード（さらに0.1秒間隔）
   - 合計で2秒以上かかる場合がある

### アニメーションの総数

- **KpiSection**: 1つのstaggerアニメーション（3カードを順次）
- **FeatureCard（1枚あたり）**: 
  - カード全体: 1
  - アイコン: 1
  - ドット: 3
  - タイトル: 1
  - アンダーライン: 1
  - 説明文: 1
  - **合計: 8個のアニメーション**
- **StrengthsSection**: FeatureCard × 2枚 = 16個
- **NavigationCardsSection**: 
  - セクションタイトル要素: 4個
  - グリッド: 1個
  - 個別カード: 3個（各カード内にアンダーライン含む）
  - **合計: 8個以上**
- **Footer**: 4個

**ページ全体で**: 約30個以上の表示アニメーションが段階的に実行される

---

## 推奨される改善案

### オプション1: 一括表示（推奨）
- 各セクション全体を一度に表示
- 内部要素の個別アニメーションを削除
- シンプルで速い表示

### オプション2: 最小限の段階的表示
- セクション単位で一括表示
- カード間のstaggerを削除
- 内部要素のアニメーションを削除

### オプション3: カスタマイズ可能
- アニメーション有効/無効を選択可能
- prefers-reduced-motion への対応

---

## 詳細なタイミング表

| コンポーネント | アニメーション数 | 最大表示時間 | 段階数 |
|--------------|----------------|------------|--------|
| KpiSection | 1 (stagger) | ~0.9秒 | 3段階 |
| FeatureCard（1枚） | 8 | ~1.3秒 | 6段階 |
| StrengthsSection | 16 | ~1.3秒×2 | 12段階 |
| NavigationCardsSection | 8+ | ~1.8秒 | 6段階 |
| Footer | 4 | ~0.8秒 | 4段階 |

**ページ全体**: 約37個以上のアニメーション、最大表示時間は約5秒以上

