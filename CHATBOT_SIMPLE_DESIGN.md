# チャットボット シンプル設計

## 新しい設計方針

### 基本コンセプト
**右下に常に「Star seeds AIに相談」という細めのボタンが常駐 → 1クリックでチャットを開く**

### 現在の問題点
1. 折りたたみボタンとメインボタンの2段階操作
2. 展開→クリックという2ステップが必要
3. 位置計算が複雑で見切れる問題が発生

### 新しい設計

#### 1. ボタン配置
```
右下固定: position: fixed; bottom: 1rem; right: 1rem;
```
- シンプルに`right: 1rem`で固定
- 余計な幅制限や計算を削除
- モバイル/デスクトップ共通

#### 2. ボタンデザイン
- **細めのボタン**: 高さ`3rem`、幅はコンテンツに合わせて自動
- **常時表示**: 折りたたみ機能なし
- **テキスト**: 「Star seeds AIに相談」
- **アイコン**: アバター画像（左側）

#### 3. インタラクション
- **1クリックでチャットウィンドウを開く**
- チャットウィンドウが開いている時はボタンを非表示（または小さく）
- チャットウィンドウは右側からスライドイン（モバイルは全画面）

#### 4. 実装の簡素化
- 折りたたみ状態の管理を削除
- `collapsed`/`expanded`/`chat-open`の3状態 → `open`/`closed`の2状態
- 複雑な位置計算を削除
- シンプルな`fixed`配置のみ

## 実装方針

### コンポーネント構造
```
Chatbot (親)
  ├─ ChatbotButton (常駐ボタン)
  └─ ChatbotWindow (チャットウィンドウ)
```

### 状態管理
```typescript
const [isOpen, setIsOpen] = useState(false);
```

### スタイル
```css
/* ボタン */
position: fixed;
bottom: 1rem;
right: 1rem;
z-index: 9999;
```

## メリット
1. **UX向上**: 1クリックでチャット開始
2. **コード簡素化**: 状態管理と位置計算が大幅に削減
3. **保守性向上**: シンプルな構造で理解しやすい
4. **パフォーマンス**: 不要なアニメーションや計算を削除

