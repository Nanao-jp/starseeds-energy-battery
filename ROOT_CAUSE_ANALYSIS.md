# 問題の原因まとめ

## 発生した問題

1. **ヒーローセクションの動画が表示されない**
2. **ヒーローセクションのテキストが日本語になっていた**
3. **「さらに詳しく」セクションが6つのカードになっていた（元は3つ）**
4. **ヘッダーの会社名が「スターシーズ株式会社」になっていた**

## 根本原因

### リファクタリング時の定数ファイルの整理

`src/lib/constants.ts`ファイルをリファクタリングする際に、以下の問題が発生しました：

1. **Git履歴を確認せずに再定義**
   - `HERO_CONFIG`や`HOME_NAVIGATION_CARDS`などの定数が削除された後、再追加する際に元の設定を確認せずに新しい値を設定してしまった

2. **実際のファイル構造を確認せずにパスを設定**
   - 動画パス: `/videos/hero-mobile.webm`（存在しないディレクトリ）
   - 画像パス: `/images/home/nav-*.webp`（実際は`/images/nav-*.webp`）

3. **意図的な変更なのか確認せずに変更**
   - カード数を3つ→6つに増やした
   - タイトルや説明文を変更した
   - 会社名を変更した

## 具体的な流れ

### 1. HERO_CONFIGの問題

**元の設定（コミット 67ce8ac）:**
- テキスト: `"Driven by Nature,"` / `"Empowered by Technology."`
- 動画パス: `/video/hero.mp4`
- 設定値: `typing.speed: 60`, `glowDelay: 300`, `fadeDuration: 3000`

**リファクタリング後（間違った設定）:**
- テキスト: `"持続可能なエネルギー社会へ"` / `"スターシーズの未来"`（日本語）
- 動画パス: `/videos/hero-mobile.webm`（存在しないディレクトリ）
- 設定値: `typing.speed: 100`, `glowDelay: 500`, `fadeDuration: 2000`

**問題:** Git履歴を確認せずに、推測で設定を追加してしまった

### 2. HOME_NAVIGATION_CARDSの問題

**元の設定（コミット f684033）:**
- カード数: 3つ
- タイトル: 「事業紹介」「製品・技術」「実績・工事状況」
- 画像パス: `/images/nav-solutions.webp`, `/images/photo/photo-02.webp`, `/images/nav-status.webp`

**リファクタリング後（間違った設定）:**
- カード数: 6つ（3つ追加）
- タイトル: 「ソリューション」「製品・サービス」「実績・工事状況」「ニュース」「会社情報」「お問い合わせ」
- 画像パス: `/images/home/nav-*.webp`（別のパス）

**問題:** 元の設定を確認せずに、拡張した設定を追加してしまった

### 3. SITE_CONFIGの問題

**元の設定:**
- `name: "Star seeds Energy"` または類似の英語表記

**リファクタリング後（間違った設定）:**
- `name: "スターシーズ株式会社"`

**問題:** 会社名を日本語に変更してしまった（意図的な変更か確認せず）

## なぜこのような問題が発生したか

### 技術的な理由

1. **定数ファイルの一元化**
   - リファクタリング時に複数のファイルに散らばっていた定数を`src/lib/constants.ts`に集約
   - その際、元のファイルを削除してから再定義する過程で、元の値を確認せずに新しい値を設定

2. **型安全性の確保**
   - TypeScriptの型定義は正しく作成されたが、値の内容が間違っていた

3. **コンパイルエラーの不在**
   - 値の内容が間違っていても、型定義が正しければコンパイルエラーは発生しない
   - 実行時まで問題が発見されない

### プロセス上の理由

1. **Git履歴の確認不足**
   - リファクタリング前に`git log`や`git show`で元の設定を確認すべきだった

2. **実際のファイル構造の確認不足**
   - 動画や画像のパスを設定する前に、実際のファイル構造を確認すべきだった

3. **意図の確認不足**
   - 変更が意図的なものか、単なるミスかを確認すべきだった

## 防止策

### 今後気をつけるべきこと

1. **リファクタリング前の確認**
   - Git履歴を確認して元の値を把握する
   - 実際のファイル構造を確認する

2. **段階的な変更**
   - 一度に全てを変更せず、段階的に変更して動作確認する

3. **定数の再定義時**
   - 元の値をコメントで残す
   - 変更理由を明確にする

4. **テストの実施**
   - リファクタリング後、各ページが正しく表示されるか確認する

## 今回の修正

1. ✅ `HERO_CONFIG`を元の設定に戻した
2. ✅ `HOME_NAVIGATION_CARDS`を元の設定（3つのカード）に戻した
3. ✅ `SITE_CONFIG.name`を「Star seeds Energy」に戻した
4. ✅ 実績・工事状況ページには影響していないことを確認

